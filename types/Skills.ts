export type DeveloperInfoType = {
  devInfo: {
    title: string
    iconName: string
    excerpt: {
      text: string
    }[]
  }
}

export type MySkillsType = {
  mySkills: {
    title: string
    iconName: string
    skills: {
      title: string
    }[]
  }[]
}
